config {
    name: "CURRNT_TEST",
  type: "table"
}

SELECT *, "${JSON.parse(dataform.projectConfig.vars.partitions).ANG}", ERROR("Dummy Error") AS PARAM FROM ${ref("TEST")}
WHERE ID >= ${dataform.projectConfig.vars.id}
